# Hatch Game Engine Example Projects

This repository contains example projects for the Hatch Game Engine. The branch you're in, `main`, does not contain an example project; instead, navigate through the other branches to find them.