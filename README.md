# `mouse-modes-test`

This demo draws a sprite under the system's cursor, and allows switching between the available mouse modes.

Controls:
- Left mouse button: Switch between `MOUSEMODE_DEFAULT` and `MOUSEMODE_RELATIVE`.
