# `mouse-modes-test`

This demo draws a sprite under the system's cursor, and allows switching between the available mouse modes.

Controls:
- Left mouse botton: Cycle between `MOUSEMODE_DEFAULT`, `MOUSEMODE_RELATIVE`, and `MOUSEMODE_CONSTRAINED`. The default is `MOUSEMODE_DEFAULT`.
